name: Windows real-machine setup E2E

on:
  pull_request:
    paths:
      - setup_win.bat
      - compose.yaml
      - .github/workflows/windows-real-machine-e2e.yml
  workflow_dispatch:
  schedule:
    - cron: "0 18 * * *"

jobs:
  windows-real-machine-setup:
    runs-on: [self-hosted, Windows, X64]
    timeout-minutes: 90
    env:
      DOCKER_BIN: C:\Program Files\Docker\Docker\resources\bin\docker.exe
    steps:
      - uses: actions/checkout@v4

      - name: Show Docker environment
        shell: powershell -NoProfile -ExecutionPolicy Bypass -Command ". '{0}'"
        run: |
          & $env:DOCKER_BIN --version
          & $env:DOCKER_BIN compose version
          & $env:DOCKER_BIN info

      - name: Clean previous run
        shell: powershell -NoProfile -ExecutionPolicy Bypass -Command ". '{0}'"
        run: |
          & $env:DOCKER_BIN compose down -v --remove-orphans 2>$null
          if ($LASTEXITCODE -ne 0) {
            Write-Host "No previous compose stack to clean."
          }

      - name: Run setup_win.bat
        shell: cmd
        run: |
          chcp 65001
          set "PATH=C:\Program Files\Docker\Docker\resources\bin;%PATH%"
          call setup_win.bat --non-interactive --openai-api-key sk-test --gemini-api-key AIza-test

      - name: Wait for services
        shell: powershell -NoProfile -ExecutionPolicy Bypass -Command ". '{0}'"
        run: |
          $targets = @(
            "http://localhost:4000",
            "http://localhost:3000",
            "http://localhost:8000/docs"
          )

          foreach ($target in $targets) {
            $deadline = (Get-Date).AddMinutes(10)
            do {
              try {
                Invoke-WebRequest $target -UseBasicParsing -TimeoutSec 5 | Out-Null
                Write-Host "$target is reachable"
                break
              } catch {
                Start-Sleep -Seconds 5
              }
            } while ((Get-Date) -lt $deadline)

            if ((Get-Date) -ge $deadline) {
              throw "$target did not become reachable"
            }
          }

      - name: Show compose state
        if: always()
        shell: powershell -NoProfile -ExecutionPolicy Bypass -Command ". '{0}'"
        run: |
          & $env:DOCKER_BIN compose ps
          & $env:DOCKER_BIN compose logs --tail=100 api
          & $env:DOCKER_BIN compose logs --tail=100 admin
          & $env:DOCKER_BIN compose logs --tail=100 public-viewer

      - name: Clean up
        if: always()
        shell: powershell -NoProfile -ExecutionPolicy Bypass -Command ". '{0}'"
        run: '& $env:DOCKER_BIN compose down -v --remove-orphans'
